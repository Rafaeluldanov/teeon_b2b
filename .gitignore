# dependencies
node_modules/

# next.js
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# env
.env
.env.local
.env.*.local
.env.local.save

# runtime / uploads
.tmp/
public/uploads/

# server-side data: leads PII, admin dumps, MinIO backups
leads/
backups/

# webp migration artifacts (mapping files generated by scripts/migrate-minio-to-webp.mjs)
scripts/.webp-mapping*.json

# tests / reports
test-results/
playwright-report/
audit-reports/

# local binaries (e.g. MinIO single-binary launcher)
minio

# misc
.DS_Store
*.log

# Личные настройки Claude Code (хуки, скрипты автокоммита) — не таскаем в репо
/.claude/
